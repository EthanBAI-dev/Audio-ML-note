# 改写对照实验

放试写稿，供和原稿对比。**这个目录里的文件都不是正式稿**，正式稿在 `零基础版_*/` 里，目前仍是原稿。

## 目录

| 目录 | 内容 | 用的什么方法 |
| --- | --- | --- |
| `原稿/` | 01–06 六篇改写前的样子 | — |
| `B-教程式/` | 01–06 六篇 | `beginner-friendly-technical-tutorial` |

另有 `../khazix-writer对照实验/`，是更早一次用 khazix-writer 风格对第 01 课的试写。

曾经有一版 `A-叙事主干`（ABT 叙事主干），效果不好，已撤掉；对应的 `references/narrative-spine.md` 和检查器里的叙事主干检查也一并删除。

## B 教程式改了什么

固定十三段，逐篇套用：

```
一句话定义 → 核心价值 → Before/After → 类比
  → Why + 对照表 → 心智模型
  → 核心概念（每个都带「常见错误」）
  → 环境 → Hello World（Step 1/2/3）
  → Level 1…N 逐层加码
  → 工程注意 → FAQ ×4 → 速查表 → 学习路径
```

三条最明显的变化：

1. **每段代码都有实测输出**，不是示意。数字全部在 Python 3.12.10 / numpy 2.5.2 / librosa 1.0.0 上真跑出来的。
2. **每个概念后面跟一条「常见错误」**，写清楚错了会怎样、为什么不报错。
3. **多了 Before/After、对照表、FAQ、速查表**，让文章同时能当参考手册用。

篇幅代价：

| 课 | 原稿 | 教程式 | 倍数 |
| --- | --- | --- | --- |
| 01 | 12,959 | 16,839 | 1.3× |
| 02 | 11,661 | 15,622 | 1.3× |
| 03 | 12,819 | 16,678 | 1.3× |
| 04 | 12,738 | 15,210 | 1.2× |
| 05 | 14,221 | 18,145 | 1.3× |
| 06 | 5,112 | 16,897 | 3.3× |

## 试写时新挖出来的可验证事实

这些是原稿里没有、写教程式版时实测出来的：

- **01**：那声敲击只让整段频谱变了 **0.17%**，在声谱图上却是相邻列的 **11.4 倍**。「证据被抹掉」第一次有了数字。
- **03**：RMS 精确相同的正弦和白噪声，质心 229 Hz vs 3997 Hz，**LUFS 差 3.6**。
- **04**：3 Hz、7 Hz、13 Hz 在 fs=10 下的采样值逐个列出来——3 和 13 完全相同，7 是它的相反数。「无法区分」不再是形容词。
- **05**：把六段能量倒序，均值和标准差**一位小数都不差**。
- **06**：同一帧加 Hann 窗后 RMS 从 0.05299 掉到 0.03413，掉了三分之一。

## 已知的取舍

- 长度增加 20%–30%（第 06 课因为原稿本来就短，涨了 3.3 倍）。更像文档，适合边看边敲，不适合一口气读完。
- 原 skill 的 §6（API Key、`.env`）和 §10（Async、Security、Deployment）是给软件教程设计的，DSP 课用不上，已在 skill 末尾注明按实际情况裁掉。
- **和零基础规则有一处真冲突**：skill 要求「开头 200 字内说清是什么」，而零基础规则要求「前两段不出现未解释术语」。01 和 03 的「这是什么」一节因此各留了一个 WARN（一段里引入 3 个和 6 个术语）。处理办法是每个术语当场加粗并配一句话解释，ERROR 已清零。
- **第 10 课那种纯建立直觉的课要小心**：这套 skill 要求「最小可运行 Demo + 完整输出」，可能会逼出并不必要的代码。

## 下一步

用户已确认「教程式方向对」，但标题、配图、以及两套 skill 的合并都还要改。
详见 [下一步待办.md](下一步待办.md)。

## 定稿之后要做的事

1. 把 `B-教程式/` 里的文件覆盖回 `零基础版_01-05/` 和 `零基础版_06-10/`。
2. **改图片和链接路径**：草稿里写的是 `../../零基础版_01-05/figures/…`，回到正式位置后应该是 `figures/…`。

   ```bash
   sed -i 's#\.\./\.\./零基础版_01-05/figures/#figures/#g; s#\](\.\./\.\./零基础版_01-05/#](#g' 零基础版_01-05/0*.md
   ```
3. 跑一遍 `check-readability.mjs`、`check-markdown-math.mjs`、`check-links.mjs`。
