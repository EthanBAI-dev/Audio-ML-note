# 下一步待办

> 更新于 2026-09-04。01—10 已按 PPT / notebook 重写完并推送。

## 已完成：01—10

十篇都做了同一件事——**先读 `PPT内容/NN-*.md` 的逐页全文，照它的顺序写**，
而不是先摆好栏目再往里塞内容。每篇的偏差和修法记在
[`PPT脉络与内容归属表.md`](PPT脉络与内容归属表.md) 的「当前草稿的主要偏差」一列。

文件名全部改成和内容一致（旧名如 `01-波形频谱与声谱图-…` 和这一课的内容
正好相反）。链接跟着改过，`node tools/check-links.mjs` 59 个文件全通过。

**检查状态**：可读性各篇 ERROR 0；公式 ERROR 0；
手机版 SVG `零基础版_01-05` 47 张、`零基础版_06-10` 56 张，都是 ERROR 0。

**这一轮实测推翻的几处自己的说法**（都已改，留作提醒）：

- 03：原稿整节讲「距离翻倍掉到四分之一」，但 PPT 全文 grep 不到 inverse /
  square / 4πr²——**平方反比是我自己加的**。
- 05：五个维度写成四个，**「音乐属性」原文出现 0 次**。
- 08：原稿说「三段的峰值都被低估了」，实测整段最大值爵士和摇滚**完全相同**。
  abs 的影响在逐帧上（18%–36% 的帧），不在全局峰值上。
- 08：原稿说「古典 0.60 最高」，实测是摇滚 0.39 最高、古典 0.29 最低，写反了。
- 09：原 notebook 用「语音 vs 噪声」说明过零率能分辨，**实测这一对分不开**
  （0.0922 对 0.1141）；倒是古典对摇滚差了近一倍。
- 图上也有：`figdata` 按小数位舍入把 1e-12 压成 0，导致 log10(0) = -Infinity；
  lesson03 打印 4% 而给配图的是 1.9%，两处用了不同公式。

## 接着做：11—23

**做法和 01—10 一样，不要变：**

1. 先读 `NotebookLM课程博客_重写版/PPT内容/NN-*.md` 的逐页全文。
2. 照它的顺序列小标题，**删任何一条都要写明理由，且理由只能是
   「归属表说它属于别的课」**。
3. 让 `课程代码/lessons/lessonNN_*.py` 先跑出数，正文只引用跑出来的数。
4. 脚本加 `--dump`，配图从 `课程代码/data/lessonNN.json` 读，
   **Node 只负责画，不算数**。
5. 每篇过四个检查：可读性、公式、链接、手机版 SVG。

**归属表目前只覆盖 01—10。** 写 11—15 之前得先把表往后补一段，
否则又会出现「这一课把下一课的内容讲完了」。

## 还欠着的事

- **正式稿没动。** `零基础版_*/` 里还是旧文章。归属表第 6 条写明
  「当前阶段不要覆盖正式稿」，迁移要等 11—23 也写完，一次性做。
- **小红书卡片版**（`tools/build-cards-*.mjs`）没有跟着这轮重写更新。
- **`01-course-problem` 之外可能还有别的孤儿 SVG**——没有生成脚本、
  改不动也复现不了。值得扫一遍 `figures/desktop/` 里哪些名字在
  `tools/*.mjs` 里搜不到。
- **代码截图**：用户提过想要，但普通代码块在手机上、搜索上、机检上都更好。
  如果是为了小红书卡片（卡片只吃图片），那只给卡片版做代码图即可——待确认。
- **PPT 原图**（比如第 01 课那只猫）：技术上能用 pymupdf 抽出来嵌进 SVG，
  但那是 Velardo 的版权素材，直接搬进要发布的文章里和「照他的结构自己重写」
  不是一回事。待用户确认。
- **`course-roadmap.svg`**（第 01 课那张「这 23 课要讲的东西」）用户说有问题，
  具体哪里不对还没问清楚。
