# 下一步待办

2026-09-02 记。第 01–06 课的教程式试写稿已完成（`B-教程式/`），用户评价：**方向对，但有三处要改**。文章本身这次不动，先把要做的记下来。

---

## 1. 两套 skill 已合并（2026-09-02 完成）

合并成一个 `audio-course-lesson`，原来的 `beginner-friendly-technical-tutorial` 已删除。

合并时处理掉的冲突：

| 冲突点 | 教程式要求 | 零基础要求 | 合并后怎么定 |
| --- | --- | --- | --- |
| 开头 | 200 字内说清「是什么」 | 前两段不出现未解释术语 | 术语当场加粗＋一句话解释，两条同时满足 |
| §6 环境 | 要 API Key、`.env` | —— | 删掉，只留 Python 版本和 `pip install`，一组只写一次 |
| §10 工程 | Async、Security、Deployment | —— | 换成边界条件、数值稳定性、性能、可复现 |
| §9 真实项目 | 另起一个完整项目 | —— | 用贯穿 23 课的《三首曲子，一个分类器》第 N 步 |
| 篇幅 | 没有上限 | 零基础读者读得完 | **仍无结论**，涨了 20%–30% |

**合并补上的最大一块是配图规则**——教程式全文只有一个词 `Diagram` 提到图，合并前那一半整个丢了。新 SKILL.md 第五节给出「哪一段该配什么」的对照表，并规定 **ASCII 块只允许用于命令行和目录树**。

这条已经变成机检：`check-readability.mjs` 现在会标出超过 40 列的 ```text 块（手机溢出），以及块里用 ↓ 画流程图的情况（该改成真图）。试写稿上标出 26 处。

## 2. 标题已按用户意见重写（2026-09-02 第二轮）

用户给的原则：**一定要说明白主语和谓语。** 生活化的例子是手段，但必须让读者知道它和主体是什么关系。**简单又明了才是追求。**

改后：

| 课 | 新标题 |
| --- | --- |
| 01 | 电脑读录音的三种方式：波形、频谱与声谱图 |
| 02 | 录音软件里那条线记下了什么？——波形、频率与音高 |
| 03 | 音量一样，为什么听起来不一样？——分贝、响度与音色 |
| 04 | 采样率和位深：电脑录音时定下的两个数 |
| 05 | 该从录音里算出什么交给模型？——音频特征的四个选择 |
| 06 | 整段录音太长，先切成小段：分帧、加窗与聚合 |

同一轮还改掉的坏词（用户逐条点名）：

| 原文 | 问题 | 改成 |
| --- | --- | --- |
| 同一段录音有三种**整理方式** | 「整理」通常指整理文件，和主题接不上 | 电脑**读**一段录音，有三种方式 |
| 可以**摆成三种样子**给程序看 | 计算机不是人，不会摆样子 | 有三种常见的**读取**方式 |
| **摆错了姿势**，关键证据就没了 | 同上 | **读取方式选错了**，你要找的那个特征就已经丢掉了 |
| 三种**摆法** / 重新**摆**一遍 | 同上 | 三种**读取方式** |

**文件名还没改**（仍是旧标题）。改文件名会牵动 23 篇里的相互链接，等定稿一起做。

**02–06 的正文还没有逐句过这一关**。这次只改了标题、导读，以及 01、05 里用户点名的句子。下次要按同样的标准通读一遍：主语谓语在不在、例子有没有连回主体。

---

## 3. 配图要跟着改（现在是旧版的图）

`B-教程式/` 里引用的还是**原稿时代的图**，路径指向 `../../零基础版_*/figures/`。图本身没问题，但**不是为教程式结构画的**。

具体对不上的地方：

- 教程式新增了 **Before/After 对比**、**心智模型流程图**、**Level 逐层加码**、**速查表**这几种段落，现在只能用 ` ```text ` 画 ASCII 流程图凑合。这些应该有真正的图。
- 原稿的图是按「三种表示 / 三个旋钮 / 四个问题」这类**概念分组**画的，教程式是按**学习步骤**推进的，两者的切分点不一样。
- Hello World 一节新出现的可验证事实还**完全没有图**，而这些恰恰是最值得画的：
  - 01：敲击让整段频谱只变 0.17%，声谱图上却是相邻列的 11.4 倍 —— 应该有一张并排对比图。
  - 04：3 Hz / 7 Hz / 13 Hz 在 fs=10 下采样值完全重合 —— 现有的 `04-aliasing.svg` 只画了两条，应该画三条。
  - 03：RMS 相同、LUFS 差 3.6 —— 没有图。
  - 06：加窗后 RMS 从 0.05299 掉到 0.03413 —— 没有图。

要做的：

1. 先定标题和结构（第 1、2 条），**再**动图。结构没定就画图会白画。
2. 定稿后按 `tools/build-figures*.mjs` 的方式重画，三版式（desktop 880 / mobile 420 / card 912）一起出。
3. 卡片版是独立的 `tools/build-cards-*.mjs`，也要跟着改。

---

## 4. 定稿时的机械操作（别忘了）

草稿在 `B-教程式/`，图片和链接路径写的是 `../../零基础版_01-05/...`，回到正式位置要改回来：

```bash
sed -i 's#\.\./\.\./零基础版_01-05/figures/#figures/#g; s#\](\.\./\.\./零基础版_01-05/#](#g' 零基础版_01-05/0*.md
```

然后跑三道检查：

```bash
node .claude/skills/audio-course-lesson/scripts/check-readability.mjs NotebookLM课程博客_重写版/零基础版_01-05
node .claude/skills/audio-course-lesson/scripts/check-markdown-math.mjs NotebookLM课程博客_重写版/零基础版_01-05
node tools/check-links.mjs
```

---

## 5. 已经定下来、不要再回头的事

- **正式稿在定稿前不许覆盖。** 所有试写都写到 `*对照实验/` 目录里。
- **不要「读完能做到」那一行**，导读就够了。已从 23 篇删除，生成它的 `tools/add-lesson-outcomes.mjs` 也删了。
- **07–23 还没开始**。第 10 课（傅里叶的直觉）要特别小心：那一课本来一行代码都没有，教程式要求「最小可运行 Demo + 完整输出」，**有逼出不必要代码的风险**，动手前先想清楚。
