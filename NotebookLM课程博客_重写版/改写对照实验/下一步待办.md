# 交接说明 · 接手的人从这里开始读

> 更新于 2026-09-04。**01—12 已按 PPT 逐页全文重写完并推送。下一篇是 13。**
>
> 这份文件是给下一个接手的账号看的。按下面的顺序读文件，按「一课的完整做法」
> 那一节干活，别自己发明流程——前面已经因为「先摆栏目再塞内容」返工过一整轮。

---

## 一、这个仓库在做什么

把 Valerio Velardo 的 23 讲音频信号处理课（原始素材在 `source_course/`），
改写成一套**中文、零基础也能读懂**的 23 篇课程文章。

- **在写的草稿**：`NotebookLM课程博客_重写版/改写对照实验/B-教程式/NN-*.md`
- **正式稿**：`NotebookLM课程博客_重写版/零基础版_01-05/` 等五个目录里的
  `零基础版_*.md`。**现在不要动正式稿**，等 13—23 全写完再一次性迁移。
- 配图 SVG 放在 `零基础版_NN-NN/figures/desktop/` 和 `.../mobile/`，
  草稿用相对路径 `../../零基础版_11-15/figures/...` 引用它们。

## 二、接手先读这五个文件（按顺序）

| 顺序 | 文件 | 读它干什么 |
|---|---|---|
| 1 | 本文件 | 流程、约定、踩过的坑 |
| 2 | `改写对照实验/PPT脉络与内容归属表.md` | **每个概念归哪一课主讲**。已覆盖 01—15，写 16 之前要先往后补 |
| 3 | `PPT内容/13-Discrete-Fourier-Transform.md` | 下一课的源材料**逐页全文**，文章的骨架就是它的顺序 |
| 4 | `改写对照实验/B-教程式/12-复数形式的傅里叶变换-把强度和起点写进一个系数.md` | 最近一篇的成品，照它的写法和密度写 |
| 5 | `课程代码/lessons/lesson12_complex_ft.py` | 最近一个脚本，照它的结构写 lesson13 |

`CLAUDE.md` / 用户 memory 里还有几条硬要求，务必遵守：
**零基础读者优先于准确性和结构**；三层结构（生活问题 → 直观解释 → 专业原理）
写进同一篇，不出两个版本；**不要写「读完能做到」那种 bullet**；
主语谓语写清楚、别拟人化；**连续轴（时间、频率）画曲线，不要画柱状图**。

## 三、一课的完整做法（六步，不要跳）

以第 13 课为例。

**第 1 步 · 读源材料逐页全文**

```bash
cat "NotebookLM课程博客_重写版/PPT内容/13-Discrete-Fourier-Transform.md"
```

这个文件是 `tools/extract-source-outline.py` 从 `source_course/` 的 PDF 和
notebook 抽出来的**每一页全文**，不是摘要。**文章的一级、二级标题就照它的顺序排。**

> 之所以强调这一条：01—10 第一版是「先摆好固定栏目、再往里塞内容」写的，
> 结果 PPT 02 有 22 页却只塞进 3 个「核心概念」槽位，03 是 32 页塞 4 个。
> 用户的原话是「跑题了的感觉」。**删源材料里的任何一条都要写明理由，
> 而唯一站得住的理由是「归属表说它属于别的课」。**

**第 2 步 · 查归属表，确认哪些概念不归这一课**

`PPT脉络与内容归属表.md` 里 13 这一行写的是：DFT 的两个 Hack、冗余、FFT 归 13；
14 只调用 `np.fft.fft`，不重讲原理。

**第 3 步 · 先写脚本，跑出真数**

```bash
cd "NotebookLM课程博客_重写版/课程代码" && python lessons/lesson13_dft.py
```

新建 `课程代码/lessons/lesson13_dft.py`，照 `lesson12_complex_ft.py` 的结构：
函数名 `step1_...`、`step2_...`，每个函数对应 PPT 的一段，`print` 出来的
段落前面标 `[正文]` 或 `[脚本额外]`。**正文里出现的每一个数字，都必须是这个
脚本真的跑出来的**，不能是估的、抄的、想当然的。

脚本开头两行是必须的（不写就会 `UnicodeEncodeError`）：

```python
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
sys.stdout.reconfigure(encoding="utf-8")
```

公共工具在 `课程代码/soundlab/`：`config.py`（SR=22050、FRAME_LENGTH=1024、
HOP_LENGTH=512、TARGET_DBFS=-20）、`io.py`（`io.load("piano_c", seconds=1.0)`）、
`framing.py`、`time_features.py`、`probe.py`、`figdata.py`。

**第 4 步 · 脚本加 `--dump`，把配图数据写成 JSON**

```bash
python lessons/lesson13_dft.py --dump
```

写一个 `dump_figures(...)`，调 `soundlab.figdata.dump(13, {...})`，
输出到 `课程代码/data/lesson13.json`。长数组先过 `thin(arr, 400)`
（按最大绝对值池化，保号）。

**规矩：Python 算，Node 只画。** 同一个量不许算两遍——
第 03 课就出过正文印 4%、配图算 1.9% 的事，两处用了不同公式。

**第 5 步 · 画图**

图加在 `tools/build-figures-11-15-ppt.mjs`（16 课起新建
`build-figures-16-20-ppt.mjs`，照同一个模子）。文件顶上
`const d13 = D('13');` 读 JSON，然后往 `FIG` 里加条目。

```bash
node tools/build-figures-11-15-ppt.mjs
```

画图函数都在 `tools/lib/tutorial-figure.mjs`：`panel()`、`curve()`、
`bars()`、`legend()`、`chain()`、`header()`、`MT()`、`T()`、`R()`、`L()`、`O()`。

**第 6 步 · 过四个检查，全部 ERROR 0 才算完**

```bash
node .claude/skills/audio-course-lesson/scripts/check-readability.mjs "NotebookLM课程博客_重写版/改写对照实验/B-教程式/13-XXX.md"
```

```bash
node .claude/skills/audio-course-lesson/scripts/check-markdown-math.mjs "NotebookLM课程博客_重写版/改写对照实验/B-教程式/13-XXX.md"
```

```bash
node tools/check-links.mjs
```

```bash
node .claude/skills/audio-course-lesson/scripts/check-svg-mobile.mjs "NotebookLM课程博客_重写版/零基础版_11-15/figures/mobile"
```

WARN 可以留，但**每条都要说得出保留理由**；ERROR 必须清零。

## 四、必须记住的六条约定（都是踩出来的）

1. **手机版 SVG 字号下限。** 手机版画板 420，显示时缩到 360，即 0.857 倍；
   有效字号下限 11.5 px，所以图里文字**至少 13.5 px，直接写 14**。
   约定写法：`const tiny = (M) => (wide(M) ? 12.5 : 14);`
   写死 11.5 的话缩完只剩 9.9 px，`check-svg-mobile` 会判不合格。
2. **` ```text ` 块每行不超过 40 列**，否则手机上横向溢出。
3. **`$...$` 里不许出现中文**，`check-markdown-math` 会报错。
4. **`figdata._plain` 按有效数字舍入，不是按小数位。** 按小数位会把 1e-12
   压成 0，第 03 课的听阈就是这么变成 0 的，配图上算出 `log10(0) = -Infinity`。
5. **`curve()` 不给 `xr` 时，数据均匀铺满整块画板宽度。** 不要以为它会去查
   `panel` 的 `xr`——踩过一次：`curve` 按 `[0, n-1]` 调 `sx`，而 `panel` 默认
   `xr: [0,1]`，结果除第一个点外全被推到画板右边几百倍远，画出来是条平线。
6. **改名脚本只许动 `改写对照实验/`。** 有一次顺手把 `零基础版_*` 里的链接
   也改了，可那些链接指向的是还没改名的正式稿，一下多出 4 条死链。

## 五、01—12 这一轮，实测推翻的自己的说法

留着当提醒：**先跑数，再下结论。**

- 03：原稿整节讲「距离翻倍掉到四分之一」，但 PPT 全文 grep 不到 inverse /
  square / 4πr²——**平方反比是我自己加的**。
- 05：五个维度写成四个，**「音乐属性」原文出现 0 次**。
- 08：原稿说「三段的峰值都被低估了」，实测整段最大值爵士和摇滚**完全相同**；
  abs 的影响在逐帧上（18%—36% 的帧），不在全局峰值上。
- 08：原稿说「古典 0.60 最高」，实测是摇滚 0.39 最高、古典 0.29 最低，写反了。
- 09：原 notebook 用「语音 vs 噪声」说明过零率能分辨，**实测这一对分不开**
  （0.0922 对 0.1141）；倒是古典对摇滚差了近一倍。
- 06：频谱泄漏的接缝要比 `x[K]` 和 `x[0]`（DFT 假定周期延拓），不是比
  `x[0]` 和 `x[-1]`——后者两种情况都得 0；初相还得取非零（用了 π/3），
  否则两种情况都回到 0。改完是 0.0000 对 1.7321。
- 12：重建式子最初写成 `sin`，可系数是按 `e^{-i2πft}` 定义的，配套的是
  `cos(2πft + ∠c)`。写错时「带相位」反而比「不带相位」误差还大，
  一眼就能看出式子配错了。改对之后是 2.477e-13 对 2.0481。

## 六、12 课收尾状态（已完成，供核对）

`lesson12_complex_ft.py` 跑出来的数：`c = +0.0000+1.0000i`，`|c| = 1.0000`，
`∠c = +1.5708`；两支分开算与合成一个 e 的指数算**结果一致**；
440 Hz 幅度 1.0000 相位 −1.5708，880 Hz 幅度 0.5000 相位 −1.5708；
静默处 1696 Hz 幅度 6.93e-17，此处相位读数是噪声；
带相位往返误差 **2.477e-13**，相位清零后 **2.0481**；
真实钢琴录音 rfft/irfft 往返 1.192e-07，只留幅度 3.2559。

三张图：`12-one-coefficient.svg`、`12-mag-and-phase.svg`、`12-roundtrip.svg`。
四个检查全过（可读性 ERROR 0、公式 ERROR 0、59 个文件链接全通、
`零基础版_11-15` 手机版 30 张 SVG ERROR 0）。

## 七、剩下要做的

**主线：13、14、15，然后 16—23。**
每写完 5 课推一次 GitHub（上一次推送在第 12 课）。
**写 16 之前先把 `PPT脉络与内容归属表.md` 补到 16—23**，
否则又会出现「这一课把下一课的内容讲完了」。

**欠着的事：**

- **正式稿没动。** `零基础版_*/` 里还是旧文章，等 13—23 写完一次性迁移。
- **小红书卡片版**（`tools/build-cards-*.mjs`）没跟着这轮重写更新。
- **孤儿 SVG**：`figures/desktop/` 里有些名字在 `tools/*.mjs` 里搜不到，
  也就没有生成脚本、改不动也复现不了。值得扫一遍。

**三件等用户拍板的事（别自己决定）：**

1. **代码截图**：用户提过想要，但普通代码块在手机上、搜索上、机检上都更好。
   如果是为了小红书卡片（卡片只吃图片），那只给卡片版做代码图即可——待确认。
2. **PPT 原图**（比如第 01 课那只猫）：技术上能用 pymupdf 抽出来嵌进 SVG，
   但那是 Velardo 的版权素材，直接搬进要发布的文章，和「照他的结构自己重写」
   不是一回事。待确认。
3. **`course-roadmap.svg`**（第 01 课那张「这 23 课要讲的东西」）用户说有问题，
   具体哪里不对还没问清楚。
