# 第 06～10 课改动记录

## 2026-08-28：继续 Claude Code 的零基础改写流程

### 正文

- 新建 06～10 零基础版，原稿未覆盖。
- 文件名随新标题同步修改；标题直接呈现每篇的技术重点。
- 每篇标题下增加一段简短导读，说明问题、知识链和阅读收获。
- 删除正文中的“课程来源与复现材料”，不再附课程视频、PDF 或叙事底稿清单。
- 不显示“第一层 / 第二层 / 第三层”等写作脚手架，改为按问题自然引出概念。
- 06 聚焦分帧、帧长、帧移、加窗与聚合；07 只比较 AE/RMS/ZCR；08、09 分别承担实现；10 只讲傅里叶直觉，减少与后续课程重复。
- 修正实现细节：AE 使用最大绝对值；RMS 只对实际帧长度或完整帧求平均；ZCR 明确零线阈值；时间坐标放在帧中心；尾帧策略显式化。

### 配图

- 新增 20 张知识图，输出 40 个 SVG：桌面 20 张、手机 20 张。
- 数据图来自真实课程音频或参数明确的合成信号，不使用装饰性波形代替计算结果。
- 手机版保留曲线、箭头、比较关系和说明文字，横向表格转成逐行纵向卡片。
- 目视检查后修复 07 对照表窄屏拥挤问题。
- 目视检查后修复 09 的跨录音过零率比较：先统一幅度尺度，再应用零线阈值，并在图注说明归一化目的。
- 联系表工具增加系列参数：`01-05` 仍为默认值，也可生成 `06-10` 联系表。
- 配图脚本不再从本机另一个项目借用 `sharp`；依赖改由 `tools/package.json` 独立声明，新环境先运行 `npm install --prefix tools` 即可复现。

### Skill

- `.agents/skills/audit-and-rewrite-popular-science/` 与 `.claude/skills/audit-and-rewrite-popular-science/` 同步更新。
- `data-figures.md` 增加“跨录音比较先统一尺度”：说明绝对电平、逐段归一化和幅度阈值的关系。
- `mobile-figures.md` 增加横表的语义转置规则：手机端保留所有单元格内容，不缩小、不删字。

### 验收

- `build-figures-06-10.mjs`：20 张 × 2 版式生成成功。
- 手机 SVG：20 张，ERROR 0；最小有效字号 12.0 px 或更高。
- 正文可读性检查：见《术语与可读性审计》。
- 桌面与手机联系表已人工目视检查，无裁切；具体修复记录见上文。

## 后续建议

- 下一批改写 11～15 时，先重新确认 11（复数）、12（旋转匹配）、13（DFT）、14（FFT 读数）、15（STFT）的边界，避免重复解释傅里叶直觉。
- 第 15 课的声谱图继续采用真实计算的栅格热力图 + SVG 文字，不用纯 SVG 色块模拟。
- 发布前可在实际博客主题中抽查 `<picture>` 是否按 640 px 切换；GitHub 预览主要用于检查内容和路径。
