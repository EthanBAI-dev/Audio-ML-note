# 改动记录与后续建议

更新日期：2026-08-27。

## 2026-08-27：知识点主线与手机端配图

- 5 篇导读改为明确的知识链，不再只预告场景：
  - 第 01 篇：波形 → 频谱 → 声谱图 → 采样与分帧 → 模型输入；
  - 第 02 篇：声音传播 → 波形 → 周期、噪声与瞬态 → 正弦波三参数 → 频率与音高；
  - 第 03 篇：声功率、声强与声压 → 距离衰减 → 分贝 → 响度 → 音色；
  - 第 04 篇：采样 → 采样率与混叠 → 量化与位深 → 参数取舍；
  - 第 05 篇：抽象层级 → 时间尺度 → 信号域 → 产生方式。
- 二级标题统一改为“知识点：它解决的问题或产生的结果”。第 01 篇把频谱与声谱图、采样率与分帧拆成独立章节；第 02 篇增加 MIDI 与音分的三级知识点；第 05 篇删除了较长的人物描述类比。
- 20 张 SVG 从 900 px 横向多栏图，全部重做为 420 px 竖向单列图。三项以上的流程、比较和分类均改为上下排列。
- 以 360 px 手机正文宽度验收：图内最小有效字号为 12 px；20 张图的手机检查结果为 ERROR 0、WARN 0。
- `tools/build-figure-contact-sheets.mjs` 改为按 360 px 输出窄屏联系表，输出到 `tmp/figure-contact-sheets-mobile/`；5 组联系表均已目视检查。
- skill 新增 `references/mobile-figures.md` 与 `scripts/check-svg-mobile.mjs`，并加入“知识点主线”规则。
- 本轮只修改 Markdown、SVG、生成与检查脚本以及 skill，没有继续调整网页内容。

## 2026-08-27：窄屏图案补回

- 根据手机端预览反馈，修正“版面变窄后图形被文字卡片取代”的问题。窄屏适配改为重排图形，不再以删除图案换取空间。
- 为 7 张偏文字化的图补回与知识点对应的机制图形：声音处理流程、三种声音表示保留的信息、空气振动变成数字、声功率/声强/声压、采样率与位深取舍、特征选择四问、规则计算与模型学习。
- 新增的图案包括波形、频率成分、声谱图、空气传播、麦克风、采样点、测量表、时间尺度和学习网络等；它们用来提示机制，不是无关装饰。
- 其余 13 张原本已经保留曲线、坐标、位置关系或变化过程，因此不做装饰性追加。
- 重新以 360 px 宽度生成并目视检查 5 组联系表：20 张 SVG 均无裁切和重叠，最小有效字号不低于 12 px，检查结果 ERROR 0、WARN 0。
- `.agents` 与 `.claude` 两份 skill 同步增加规则：空间不足时优先增加画布高度、改为纵向关系、让文字换行或拆图；有解释作用的文字和承载知识的图形都应保留。

## 2026-08-27：文件名与正式标题同步

- 第 01～05 篇 Markdown 文件按当前正式标题重新命名，使文件列表本身也能显示波形、频谱、音高、分贝、采样率、位深和音频特征等技术重点。
- 文件名去掉了 Windows 不允许使用的问号、冒号和引号，保留标题中的关键词与核心问题。
- 同步更新本目录文章索引与可读性检查命令中的全部链接。

## 前一轮已完成

- 第 01～05 课正文均改为自然递进：不再显示「第一层 / 第二层 / 第三层」，也不再出现「怎么读这篇文章」一类结构说明。
- 删除 5 篇文末的术语表与「课程来源与复现材料」。
- 拆掉第 01 篇「换成通用说法」式的批量术语段，把分类器、声音表示、采样率、分帧和模型放回各自真正需要的位置。
- 修正若干技术表达：
  - RMS 不再写成“平均能量”；
  - 采样两倍条件不再解释成“恰好抓一个高点和低点”；
  - dBFS 改为通常不高于 0，而不是“总是负数”；
  - 24 位录音的余量解释改为允许更保守地设置增益；
  - 训练与部署改为保证模型最终收到的处理结果一致，而不是要求原文件参数字面相同。
- 20 张配图全部重做为 SVG，并逐张目视验收。
- 修复 5 个配图排版问题：`01-pipeline` 裁切、`03-distance` 标签出界、`03-decibel` 横轴重叠、`05-four-questions` 自动断行、`05-time-scale` 标签拥挤。
- `tools/build-preview.mjs` 已直接内嵌 SVG，不再栅格化；同时修复了跨两行公式丢失第一行的问题。
- 桌面与 390 px 手机宽度均完成浏览器检查：无横向溢出，20 张图片全部加载。
- `.agents` 与 `.claude` 两份 skill 已同步：旧的 `three-layer-template.md` 被 `article-shape.md` 取代，明确“深度顺序只供写作者检查，不能写进标题”。
- 两份 skill 均通过 `quick_validate.py` 校验。
- 五篇可读性检查结果：ERROR 0，WARN 0。

## 2026-08-27：标题与导读补充

- 5 篇主标题全部重写为“技术关键词 + 读者问题”，让读者在列表页就能判断文章的技术范围：
  - 第 01 篇突出波形、频谱、声谱图与声音表示选择；
  - 第 02 篇突出波形、频率、相位与音高；
  - 第 03 篇突出分贝、响度与音色的区别；
  - 第 04 篇突出采样率、混叠、量化与位深；
  - 第 05 篇突出音频特征、抽象层级、时间尺度与模型输入。
- 每篇标题下增加两句话的内容导读：先交代本文解决的问题，再预告核心技术概念与阅读收获。
- 导读不说明“文章分几层”或“应该怎么读”，不会把内部写作结构暴露给读者。
- 同步更新本目录的文章索引、单页预览以及 `.agents`、`.claude` 两份 skill。

## 关键文件

- 文章目录：本目录下 `01-…md` 至 `05-…md`
- 配图：`figures/`
- 配图生成：`tools/build-figures.mjs`
- 配图联系表：`tools/build-figure-contact-sheets.mjs`
- Claude Code skill：`.claude/skills/audit-and-rewrite-popular-science/`
- Codex skill：`.agents/skills/audit-and-rewrite-popular-science/`
- 审计结果：`术语与可读性审计.md`

## 重建命令

```bash
node tools/build-figures.mjs
node tools/build-figure-contact-sheets.mjs
node ".claude/skills/audit-and-rewrite-popular-science/scripts/check-svg-mobile.mjs" \
  "NotebookLM课程博客_重写版/零基础版_01-05/figures"
```

## 后续建议

本轮没有影响发布的已知问题。继续改第 06 课以后时，先写知识点依赖链，再用场景和类比解释；配图默认按 360 px 手机正文宽度验收。
