// 23 课的「动手做」内容。每一条是课程项目《三首曲子，一个分类器》的一步。
//
// 由 tools/apply-exercises.mjs 追加到各篇文章末尾。改这里，不要直接改文章里的作业段。

export const PROJECT = '三首曲子，一个分类器';

export const EXERCISES = [
  {
    id: '01',
    title: '先用三种表示各看一眼',
    lead: '项目的第一步不是写代码，而是先确认"这三种风格的差别，到底体现在哪一种表示上"。',
    steps: [
      '打开 `debussy.wav`、`duke.wav`、`redhot.wav`，各听 10 秒，用自己的话写下三句话：这三段声音最明显的差别是什么。',
      '用任意工具（Audacity、librosa 都行）为每段各画三张图：波形、整段频谱、声谱图。',
      '对照你写下的那三句话，逐条标注：这个差别在哪一张图上看得见？在哪一张图上被抹掉了？',
    ],
    deliver: '一份 `notes/01-观察.md`，包含九张图和一张"差别 × 表示"的对照表。',
    check: '你应该会发现：摇滚和古典的高频差别在整段频谱上就很明显，但"节奏密不密"只有声谱图看得出来。如果三张图看起来都差不多，多半是画图时纵轴范围没统一。',
  },
  {
    id: '02',
    title: '把音频读成一串数字',
    lead: '开始建 `soundlab/` 工具包。第一个模块负责把音频文件变成后面所有步骤都能直接用的数组。',
    steps: [
      '写 `soundlab/io.py`，实现 `load(path, sr=22050)`：读入文件、合并成单声道、重采样到统一采样率，返回 `(samples, sr)`。',
      '对十段音频各跑一次，打印文件名、原始采样率、时长、最大绝对值、均方根。',
      '把结果存成 `notes/02-音频清单.md` 的一张表。',
    ],
    deliver: '`soundlab/io.py`，以及十段音频的基本信息表。',
    check: '三段音乐的时长应该都在 30 秒左右；`piano_c` 和 `violin_c` 明显更短。如果某个文件的最大绝对值超过 1.0，说明读取时没有做归一化，后面所有幅度相关的特征都会偏。',
  },
  {
    id: '03',
    title: '把响度统一，别让音量冒充风格',
    lead: '摇滚那段本来就录得更响。如果不处理，分类器学到的可能只是"谁的音量大"，而不是风格差别。',
    steps: [
      '算出三段音乐各自的均方根，换算成相对满幅的分贝值（dBFS）。',
      '在 `soundlab/io.py` 里加一个 `normalize_rms(y, target_dbfs=-20)`，把每段调整到同一个均方根电平。',
      '统一前后各画一次三段的波形，放在一起对比。',
      '在笔记里写清楚：这一步删掉了什么信息，以及为什么这里可以删。',
    ],
    deliver: '`normalize_rms` 函数，以及统一前后的对比图。',
    check: '统一之后，三段的均方根应该几乎相同（差别小于 0.1 dB），但波形的**形状**差别仍在——摇滚仍然更"满"，古典仍然起伏更大。如果形状也变得差不多了，说明你误用了逐帧归一化。',
  },
  {
    id: '04',
    title: '锁死预处理参数',
    lead: '这一步产出的不是代码，而是一份约定。它决定了后面 19 课算出来的数字能不能互相比较。',
    steps: [
      '新建 `config.py`，写入 `SR = 22050`、`MONO = True`、`TARGET_DBFS = -20`，并注明为什么选 22050（它能覆盖多少频率范围？够不够用？）。',
      '把 `soundlab/io.py` 改成从 `config.py` 读参数，不再接受散落的默认值。',
      '故意做一次错误实验：把其中一段用 8000 Hz 读进来，画出它的频谱，和 22050 Hz 的版本放在一起。',
    ],
    deliver: '`config.py`，以及那张"采样率不一致会发生什么"的对比图。',
    check: '8000 Hz 版本的频谱在 4000 Hz 处直接截断。如果你把它和 22050 Hz 的特征混进同一张表，模型会先学会"这条数据是哪台设备录的"。这正是必须锁死参数的原因。',
  },
  {
    id: '05',
    title: '切出数据集',
    lead: '三个文件不是三条数据。把每段切成 1 秒片段，才有足够多的样本去检验特征。',
    steps: [
      '写一个脚本，把三段音乐各切成不重叠的 1 秒片段，丢掉最后不足 1 秒的尾巴。',
      '生成 `dataset.csv`，每行记录：`file, start_sec, end_sec, label`（label 取 classical / jazz / rock）。',
      '统计每一类各有多少片段，确认三类数量大致均衡。',
      '按第 05 课的四个问题，写下这个任务的证据清单：要分辨风格，需要哪个抽象层级、哪种时间尺度、哪个信号域的证据？',
    ],
    deliver: '`dataset.csv`（应有 80–100 行左右），以及一份证据清单。',
    check: '三类的片段数量不应相差超过 20%。如果某一类特别少，检查是不是那个文件更短——不均衡的数据会让后面的准确率看起来虚高。',
  },
  {
    id: '06',
    title: '写出分帧函数',
    lead: '1 秒片段还是太长，一个数字概括不了。分帧是后面所有逐帧特征的共同底座。',
    steps: [
      '写 `soundlab/framing.py`，实现 `frame(y, frame_length=1024, hop_length=512)`，返回形状为 `(帧数, 帧长)` 的二维数组。',
      '用第 06 课的公式手算一遍：一个 1 秒、22050 Hz 的片段，用这组参数能切出多少帧？再用代码验证。',
      '加一个可选的 `window="hann"` 参数，对每帧乘上窗函数。',
      '把 `FRAME_LENGTH` 和 `HOP_LENGTH` 也放进 `config.py`。',
    ],
    deliver: '`soundlab/framing.py`，手算与代码结果一致。',
    check: '手算结果应该是 `1 + floor((22050 - 1024) / 512) = 42` 帧。如果代码给出 43，检查是不是补了边；如果给出 21，检查帧移是不是写成了帧长。',
  },
  {
    id: '07',
    title: '先看看三种时域特征分不分得开',
    lead: '在动手实现之前，先用现成的库快速看一眼：这三种特征里，哪一种对区分风格真的有用？',
    steps: [
      '用 librosa 对三段音乐各算一次振幅包络、均方根和过零率（这一课只借用结果，实现留到 08、09）。',
      '把三条曲线画在同一张图上，三种风格用三种颜色。',
      '对每一种特征回答：三种风格的曲线分开了吗？分开的是整体高低，还是起伏形状？',
    ],
    deliver: '三张对比图（每种特征一张），以及一段判断：哪种特征最有希望，哪种可能没用。',
    check: '过零率上，摇滚应该明显高于古典（失真吉他与镲片带来大量高频翻转）。如果三条线缠在一起，先回去检查第 03 步的响度统一是不是漏做了。',
  },
  {
    id: '08',
    title: '实现振幅包络',
    lead: '第一个自己写的特征。重点不在公式，而在边界：绝对值、尾帧、时间轴。',
    steps: [
      '在 `soundlab/time_features.py` 里实现 `amplitude_envelope(y, frame_length, hop_length)`，返回每帧的最大绝对值。',
      '同时返回每帧对应的时间（帧中心，单位秒），让曲线能和波形对齐画在一起。',
      '按第 08 课的讨论处理结尾：明确说明你是丢掉不足一帧的尾巴，还是补零，并在注释里写出理由。',
      '用同一段音频对比你的结果和 librosa 的分帧结果，确认帧数一致。',
    ],
    deliver: '`amplitude_envelope`，以及一张包络叠在波形上的图。',
    check: '包络曲线必须完全贴着波形的外缘，不能穿进去也不能浮在外面。如果包络整体偏低，多半是忘了取绝对值；如果时间轴对不齐，检查你用的是帧起点还是帧中心。',
  },
  {
    id: '09',
    title: '实现 RMS 与过零率，写出第一版特征表',
    lead: '这一步之后，`features.csv` 第一次有了内容——三个纯时域特征。',
    steps: [
      '实现 `rms(y, ...)` 和 `zero_crossing_rate(y, ...)`，都返回逐帧序列。',
      '对 `dataset.csv` 里每个片段，算出三种特征的**均值和标准差**，共 6 个数字。',
      '写出 `features.csv`：每行一个片段，列为 `ae_mean, ae_std, rms_mean, rms_std, zcr_mean, zcr_std, label`。',
      '画一张散点图：横轴 `zcr_mean`，纵轴 `rms_std`，三类用三种颜色。',
    ],
    deliver: '`features.csv` 的前 6 列，以及那张散点图。',
    check: '散点图上三类应该已经出现部分分离，尤其摇滚会聚在右侧（过零率高）。完全重合说明特征算错了或者标签串行了——用 `groupby(label).mean()` 核对一下每类的均值。',
  },
  {
    id: '10',
    title: '手写一次频率试探',
    lead: '不用任何变换库，亲手做一次"拿已知频率去比对"，理解频谱究竟是怎么来的。',
    steps: [
      '合成一段已知信号：440 Hz 与 880 Hz 两个正弦叠加，振幅分别是 1.0 和 0.5。',
      '写一个循环，让测试频率从 100 Hz 扫到 2000 Hz，每次把信号与该频率的正弦相乘再求平均，记录结果。',
      '把"测试频率 → 匹配值"画成曲线，看它在哪里出现峰。',
      '把测试信号的相位改成 `np.pi/2`，重跑一次，观察峰值发生了什么。',
    ],
    deliver: '一个能跑通的匹配脚本，以及两张曲线图（改相位前后）。',
    check: '第一次应该在 440 和 880 Hz 出现两个峰，高度比约 2:1。改相位后峰会明显变矮甚至消失——这正是第 12 课要用复数解决的问题，先把这个现象记在笔记里。',
  },
  {
    id: '11',
    title: '把强弱和起始位置放进同一个数',
    lead: '上一课那个"改了相位峰就没了"的问题，根源是一个实数装不下两件事。这一课先把工具备好。',
    steps: [
      '写几行代码：给定复数 `z = a + bi`，算出它的模和幅角，并画在复平面上。',
      '取一组复数 `z_k = A * exp(1j * (2*pi*f*t_k + phi))`，画出它们的实部随时间变化的曲线，确认那就是一条余弦。',
      '改变 `A` 和 `phi`，观察模和幅角各自跟着变了什么。',
    ],
    deliver: '`notes/11-复数.md`，包含复平面图和"模 ↔ 强弱、幅角 ↔ 起始位置"的对应说明。',
    check: '只改 `phi` 时，模应该完全不变。如果模也跟着变了，检查是不是把 `exp` 写成了 `cos`。',
  },
  {
    id: '12',
    title: '把匹配实验改成复数版',
    lead: '回到第 10 课那个脚本，用复数探针替换正弦探针，看那个"改相位就失灵"的毛病是否消失。',
    steps: [
      '把匹配式改成 `np.mean(y * np.exp(-1j * 2 * np.pi * f * t))`，得到一个复数结果。',
      '分别画出结果的模和幅角随测试频率变化的曲线。',
      '重复第 10 课那个改相位的实验：把信号相位改成 `np.pi/2`、`np.pi`，各跑一次。',
    ],
    deliver: '复数版匹配脚本，以及三次不同相位下的模曲线。',
    check: '三次的**模曲线应该几乎完全重合**，峰仍然在 440 和 880 Hz；变化的只有幅角。如果模还是随相位变，检查指数里的负号有没有漏掉。',
  },
  {
    id: '13',
    title: '把数组位置换算成 Hz',
    lead: '真正做 FFT 之前，先解决一个最容易错、错了又不报错的问题：第 k 个数到底对应多少赫兹。',
    steps: [
      '在 `soundlab/spectral.py` 里实现 `freq_axis(n_fft, sr)`，返回单边频谱每个位置对应的频率。',
      '手算验证：`n_fft=1024`、`sr=22050` 时，第 0、1、100、512 个位置分别是多少 Hz？',
      '把你的结果和 `np.fft.rfftfreq(1024, 1/22050)` 逐个比对。',
      '在注释里写清楚：为什么单边频谱的长度是 `n_fft/2 + 1` 而不是 `n_fft/2`。',
    ],
    deliver: '`freq_axis` 函数，与 numpy 完全一致。',
    check: '第 512 个位置应该正好是 11025 Hz，也就是采样率的一半。如果你的最后一个值不是 11025，多半是用 `linspace` 猜了终点——这是本课最常见的错误。',
  },
  {
    id: '14',
    title: '正确读一次频谱',
    lead: '把"能画出峰"提升到"峰的高度可以当数值用"。单边、加窗、归一化，三件事都要做对。',
    steps: [
      '实现 `spectrum(y, n_fft, sr, window="hann")`，返回 `(freqs, magnitude)`，其中幅值已按窗系数总和归一化。',
      '用一个振幅 0.7、频率 1000 Hz 的纯正弦验证：算出来的峰值应该接近 0.7。',
      '换成实际音乐片段，画出相对分贝形式的频谱（以本帧最大值为 0 dB）。',
      '在注释里写清楚这个函数返回的是幅度谱、不是功率谱，以及两者相差什么。',
    ],
    deliver: '`spectrum` 函数，纯正弦验证通过。',
    check: '纯正弦的峰值应该在 0.65–0.72 之间。如果是 0.35 左右，说明单边频谱忘了乘 2；如果是几百，说明忘了除以窗系数总和。',
  },
  {
    id: '15',
    title: '实现 STFT',
    lead: '把单张频谱扩展成"每一帧一张频谱"，得到后面所有频域特征的共同输入。',
    steps: [
      '实现 `stft(y, n_fft, hop_length, sr)`，返回形状明确的复数矩阵，并在文档字符串里写清楚哪一维是频率、哪一维是时间。',
      '对一个 1 秒片段调用它，打印 `shape`，并用第 15 课的公式手算核对行数和列数。',
      '用两组参数各跑一次：`n_fft=512` 和 `n_fft=4096`，比较得到的矩阵形状。',
      '把 `N_FFT`、`HOP_LENGTH` 写进 `config.py`。',
    ],
    deliver: '`stft` 函数，形状与手算一致。',
    check: '`n_fft=1024`、`hop=512`、1 秒 22050 Hz 时，形状应该是 `(513, 42)` 或 `(42, 513)`——取决于你的约定，但**必须和文档字符串写的一致**。这一课起，每次新函数都先打印 shape。',
  },
  {
    id: '16',
    title: '画出可比较的三张声谱图',
    lead: '声谱图是给人看的。要能比较，颜色范围、频率轴、分贝参考必须三张一致。',
    steps: [
      '把 STFT 结果取模平方得到功率，再换算成相对分贝。',
      '为三种风格各画一张声谱图，**三张必须使用同一个 `vmin`/`vmax` 和同一个频率轴范围**。',
      '再故意画一组"每张各自归一化"的版本，放在一起对比。',
      '在笔记里写下：哪些差别在统一条件下才成立，哪些是各自归一化造成的假象。',
    ],
    deliver: '两组各三张声谱图，以及一段关于比较条件的说明。',
    check: '统一条件下，摇滚那张的高频区域应该明显更亮。如果各自归一化后三张看起来差不多亮，正好说明了"归一化会抹掉绝对电平差别"。',
  },
  {
    id: '17',
    title: '造一组梅尔滤波器',
    lead: '513 个频率位置太多，而且高频那一段人耳本来就分不细。这一步把它们按听感重新分组。',
    steps: [
      '在 `soundlab/mel.py` 里实现 `mel_filterbank(n_mels, n_fft, sr, fmin=0, fmax=None)`，返回形状为 `(n_mels, n_fft//2+1)` 的矩阵。',
      '把这个矩阵画出来：每一行是一个三角形，看它们怎样从密到疏地铺开。',
      '用 `librosa.filters.mel` 生成一份对照，比较两者的形状与总和。',
      '回答：为什么低频那几个三角形又窄又密，高频那几个又宽又疏？',
    ],
    deliver: '`mel_filterbank` 函数，以及那张滤波器组图。',
    check: '相邻三角形应该互相重叠一半，整体从左到右越来越宽。如果所有三角形一样宽，说明你在赫兹上等分了，而不是在梅尔刻度上等分。',
  },
  {
    id: '18',
    title: '提取对数梅尔频谱',
    lead: '把滤波器组接到 STFT 后面，得到第一份真正意义上的"模型输入"。',
    steps: [
      '实现 `log_mel(y, ...)`：STFT → 功率 → 乘滤波器矩阵 → 取对数。',
      '打印每一步的 `shape`，确认矩阵乘法的方向没写反。',
      '为三种风格各画一张对数梅尔频谱图，行数应该从 513 降到 64。',
      '把这次用到的**全部参数**写进 `notes/18-参数.md`：`sr, n_fft, hop_length, n_mels, fmin, fmax, power, 对数底数, 参考值`。',
    ],
    deliver: '`log_mel` 函数，以及那份参数清单。',
    check: '输出应该是 `(64, 42)`。如果报形状不匹配，把两个矩阵的 shape 都打出来——十有八九是需要转置其中一个。参数清单必须完整到别人照着能复现同一张图。',
  },
  {
    id: '19',
    title: '看看 DCT 到底压掉了什么',
    lead: 'MFCC 只保留前 13 个系数。这一步的目的不是实现它，而是亲眼确认"扔掉后面那些"到底损失了多少。',
    steps: [
      '取一帧对数梅尔谱，对它做 DCT，把 64 个系数按大小画成柱状图。',
      '只保留前 13 个系数，其余置零，再做逆 DCT 还原成 64 维，和原始曲线画在一起。',
      '把保留数量改成 5、13、26、40，各还原一次，观察曲线什么时候开始明显失真。',
    ],
    deliver: '一张"保留不同数量系数"的还原对比图。',
    check: '保留 13 个时，还原曲线应该抓住了整体起伏但丢掉了细小的锯齿。如果保留 13 个就几乎完全重合，说明你选的那一帧太平滑，换一帧高频丰富的再试。',
  },
  {
    id: '20',
    title: '算出 39 维 MFCC',
    lead: '把静态系数和变化量拼在一起，这是这套课程里维度最高、也最常用的一份特征。',
    steps: [
      '实现 `mfcc39(y, ...)`：13 维 MFCC + 13 维一阶差分 + 13 维二阶差分，拼成每帧 39 维。',
      '按第 20 课的讨论处理边界：居中差分会用到后一帧，第一帧和最后一帧怎么算？在注释里写明。',
      '对 `dataset.csv` 每个片段，取 39 维的**逐帧均值**，加进 `features.csv`。',
      '此时 `features.csv` 应有 6 + 39 = 45 列特征。',
    ],
    deliver: '`mfcc39` 函数，以及扩充后的 `features.csv`。',
    check: '39 维中，第 1 维（第 0 号系数）数值应该明显大于其余——它代表整帧的总能量。如果所有维度量级相近，检查是不是漏了对数那一步。',
  },
  {
    id: '21',
    title: '想清楚再算：三个统计量各回答什么',
    lead: '这一步只写字不写代码。三个数字看起来都在描述"频谱长什么样"，但它们回答的是不同的问题。',
    steps: [
      '用一句话分别写出：带能量比、频谱质心、频谱带宽，各自回答关于三种风格的什么问题。',
      '对每一个，预测三种风格的相对大小（谁高谁低），并写下理由。',
      '在 `notes/21-预测.md` 里记下这三条预测——第 23 课会用实际数据验证。',
    ],
    deliver: '三条带理由的预测。',
    check: '一个合理的预测是：摇滚的质心最高、带宽最大，古典最低最窄。如果你说不出理由，回去重读第 21 课"三个数字不能互相代替"那一节。',
  },
  {
    id: '22',
    title: '实现带能量比',
    lead: '这个特征本身不难，难的是"2000 Hz 落在第几个频率箱"这一步——算错了不会报错，只会悄悄给出错误的数。',
    steps: [
      '实现 `band_energy_ratio(spec, sr, n_fft, split_hz=2000)`，返回逐帧的低频能量与高频能量之比。',
      '用第 13 课的 `freq_axis` 找分界位置，**不要**用 `int(split_hz / sr * n_fft)` 这类近似写法。',
      '手工验证一次：构造一个只有 500 Hz 的信号和一个只有 5000 Hz 的信号，比值应该分别趋近于很大和很小。',
      '把逐帧结果的均值加进 `features.csv`。',
    ],
    deliver: '`band_energy_ratio` 函数，两个极端信号验证通过。',
    check: '纯 500 Hz 信号的比值应该在 100 以上，纯 5000 Hz 的应该在 0.01 以下。如果两者都接近 1，说明分界位置算错了，或者矩阵的频率轴取反了。',
  },
  {
    id: '23',
    title: '合并全部特征，检验能不能分开',
    lead: '最后一步。把 23 课积累的东西全部接上，回答那个从第 01 课就提出的问题：这些数字，真的够程序分辨三种风格吗？',
    steps: [
      '实现 `spectral_centroid` 和 `spectral_bandwidth`，注意先声明用幅度还是功率作权重，并与 librosa 的结果对照。',
      '把两者的均值加进 `features.csv`，此时应有 48 列特征。',
      '画一张散点图：横轴质心均值，纵轴带宽均值，三类三色。回到第 21 课那三条预测，逐条核对是对是错。',
      '把 `features.csv` 按 7:3 分成两份，用最近邻做一次判断，算出准确率。（这不是在教机器学习，只是给"特征够不够用"一个参考数字。）',
      '做一次特征消融：只用 6 个时域特征、只用 39 维 MFCC、只用 3 个频域统计量，各算一次准确率。',
      '写 `report.md`：三种风格在哪些特征上差别最大，哪一组特征贡献最多，以及哪些差别是特征本身带来的、哪些可能是数据太少造成的。',
    ],
    deliver: '完整的 `features.csv`、散点图、消融结果表和 `report.md`。',
    check: '三类在质心—带宽平面上应该有明显但不完全的分离。准确率大概率在 80% 以上——但更重要的是消融表：如果只用 6 个时域特征就能到 90%，那说明这份数据太容易，应该换更接近的风格重做一次。**能解释结果，比准确率高更重要。**',
  },
];
